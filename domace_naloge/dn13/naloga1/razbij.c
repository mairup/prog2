#include <stdio.h>

int main(int argc, char *argv[])
{
    FILE *dat = fopen(argv[1], "rb");
    int k = atoi(argv[2]);
    FILE* temp;
    unsigned char byte = fread(&byte, 1, 1, dat);
    while ()
    {
        
    }
    return 0;
}