#include <stdio.h>

int tab[10];

int uredi()

    int main()
{
    int n, k, r;
    scanf("%d %d %d", &n, &k, &r);

    for (int i = 0; i < n; i++)
    {
        scanf("%d", &tab[i]);
    }

    return 0;
}